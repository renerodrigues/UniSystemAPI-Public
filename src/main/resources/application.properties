# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.url=${DATABASE_URL}
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

#  automaticamente atualiza o banco de dados

spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql: false

spring.servlet.multipart.max-request-size=1000MB
server.address=${ENDERECO_INTERNO}
uni.api.serverAddress=${ENDERECO_SERVIDOR}

uni.api.security.token.secret=${JWT_SECRET:39824thu}
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.show_sql=tue
